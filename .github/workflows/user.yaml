name: User CI

# Trigger the workflow on push or pull request to main,
# but only if files inside user/ or the workflow file itself change
on:
  push:
    branches:
      - main
    paths:
      - 'user/**'
  pull_request:
    branches:
      - main
    paths:
      - 'user/**'

jobs:
  build-and-deploy:
    name: Build, Push & Deploy User
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Docker Login
        run: |
          echo ${{ secrets.DOCKERHUB_TOKEN }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/robotshop:user-${{ github.run_id }} ./user

      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/robotshop:user-${{ github.run_id }}
